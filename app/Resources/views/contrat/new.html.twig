{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création d'un contrat</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" class="btn btn-default" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('contrat_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
