{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editer un message</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Editer" class="btn btn-default" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('conversation_show' , { 'id': message.idConversation.id }) }}">Retour à la conversation</a>
        </li>
    </ul>
{% endblock %}
